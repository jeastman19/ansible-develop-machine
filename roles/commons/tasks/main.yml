---
  - name: Instalar tree
    apt:
        name: tree
        update_cache: yes
        state: latest

  - name: Instalar git
    apt:
      name: git
      update_cache: yes
      state: latest

  - name: Instalar curl
    apt:
      name: curl
      update_cache: yes
      state: latest

